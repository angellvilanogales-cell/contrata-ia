import {describe,expect,it} from "vitest";import {evaluateDataProtectionSecurity,type DataProtectionSecurityInput} from "../src/application/intake/lb119/DataProtectionSecurityEngine";
const base:DataProtectionSecurityInput={dataScenario:"NO_ACCESS",scenarioJustification:"La entrega no requiere acceder a aplicaciones ni soportes con datos personales.",processingPurpose:"",personalDataCategories:"",dataSubjectCategories:"",processingOperations:"",specialCategoryOrCriminalData:false,processorAgreementIncluded:false,confidentialityRegime:"deber de secreto y comunicación de accesos fortuitos",returnOrDeletionRegime:"",serversOrAssociatedServices:false,serverLocationRegime:"",subprocessorsForeseen:false,subprocessorRegime:"",internationalTransfers:false,internationalTransferRegime:"",systemsSupportPublicService:false,ensApplicable:false,ensCategory:"",ensConformityEvidence:"",securityMeasuresRegime:"control de acceso, custodia y canales seguros",incidentAndContinuityRegime:"notificación inmediata, evidencias y continuidad",securityContactRegime:"interlocutor de la contratista y responsable del contrato"};
describe("LB119 · protección de datos y seguridad",()=>{it("documenta la no aplicación",()=>{const r=evaluateDataProtectionSecurity(base);expect(r.processorAgreementRegime).toMatch(/No procede/);expect(r.ensRegime).toMatch(/No se activa/);expect(r.legalBasis.map(x=>x.id)).toContain("LCSP-122.2-DATA");});it("exige anexo y elementos del encargo",()=>{expect(()=>evaluateDataProtectionSecurity({...base,dataScenario:"PROCESSOR",scenarioJustification:"El soporte opera registros por instrucciones de la Administración."})).toThrow(/artículo 28 RGPD/);expect(evaluateDataProtectionSecurity({...base,dataScenario:"PROCESSOR",scenarioJustification:"El soporte opera registros por instrucciones.",processorAgreementIncluded:true,processingPurpose:"resolver incidencias",personalDataCategories:"identificación y trazas",dataSubjectCategories:"personal y usuarios",processingOperations:"consulta, conservación y supresión",returnOrDeletionRegime:"devolución y supresión certificada"}).processorAgreementRegime).toContain("instrucciones");});it("controla servidores y subencargados",()=>{expect(()=>evaluateDataProtectionSecurity({...base,serversOrAssociatedServices:true})).toThrow(/ubicación/);expect(()=>evaluateDataProtectionSecurity({...base,subprocessorsForeseen:true})).toThrow(/servidores/);});it("no permite descartar ENS para sistemas del servicio",()=>{expect(()=>evaluateDataProtectionSecurity({...base,systemsSupportPublicService:true})).toThrow(/ENS/);expect(evaluateDataProtectionSecurity({...base,systemsSupportPublicService:true,ensApplicable:true,ensCategory:"MEDIUM",ensConformityEvidence:"Certificación vigente"}).ensRegime).toContain("no ha sido inferida");});it("rechaza mezclar acceso incidental y encargo",()=>{expect(()=>evaluateDataProtectionSecurity({...base,dataScenario:"INCIDENTAL_ACCESS",processorAgreementIncluded:true})).toThrow(/solo proceden/);});});
