import {describe,expect,it} from "vitest";import {evaluateSpecialExecutionConditions} from "../src/application/intake/lb113/SpecialExecutionConditionsEngine";
const condition={title:"Residuos",category:"ENVIRONMENTAL" as const,obligation:"Retirar embalajes de cada entrega.",objectLinkReason:"Se producen directamente en las entregas.",verificationMethod:"Control en recepción.",evidenceRequired:"Albarán y justificante.",consequence:"PENALTY" as const,consequenceDetail:"Penalidad proporcional definida en PCAP."};
describe("LB113 · condiciones especiales",()=>{it("exige una condición y genera declaraciones expresas",()=>{expect(()=>evaluateSpecialExecutionConditions({contractType:"SUPPLY",transfersPublicData:false,publicInstitutionFoodService:false,conditions:[]})).toThrow(/al menos una/);const r=evaluateSpecialExecutionConditions({contractType:"SUPPLY",transfersPublicData:false,publicInstitutionFoodService:false,conditions:[condition]});expect(r.documentaryStatements).toHaveLength(4);expect(r.documentaryStatements.find(x=>x.element==="DATA_TRANSFER_CONDITION")).toMatchObject({status:"NOT_APPLICABLE"});expect(r.documentaryStatements.find(x=>x.element==="SUBCONTRACTORS")?.text).toContain("subcontratistas");});it("exige protección de datos esencial cuando hay cesión",()=>{expect(()=>evaluateSpecialExecutionConditions({contractType:"SERVICE",transfersPublicData:true,publicInstitutionFoodService:false,conditions:[condition]})).toThrow(/protección de datos/);const data={...condition,title:"Datos",category:"DATA_PROTECTION" as const,consequence:"PENALTY" as const};expect(()=>evaluateSpecialExecutionConditions({contractType:"SERVICE",transfersPublicData:true,publicInstitutionFoodService:false,conditions:[data]})).toThrow(/obligación contractual esencial/);});it("exige prevención del desperdicio en alimentación pública",()=>{expect(()=>evaluateSpecialExecutionConditions({contractType:"SERVICE",transfersPublicData:false,publicInstitutionFoodService:true,conditions:[condition]})).toThrow(/desperdicio alimentario/);});it("rechaza condiciones incompletas y duplicadas",()=>{expect(()=>evaluateSpecialExecutionConditions({contractType:"SUPPLY",transfersPublicData:false,publicInstitutionFoodService:false,conditions:[{...condition,verificationMethod:""}]})).toThrow(/verificación/);expect(()=>evaluateSpecialExecutionConditions({contractType:"SUPPLY",transfersPublicData:false,publicInstitutionFoodService:false,conditions:[condition,condition]})).toThrow(/duplicada/);});});
