import {describe,expect,it} from "vitest";import {LB115_PLANNED_MODIFICATION_SCRIPT} from "../src/interfaces/lb103/LB115PlannedModificationScript";
describe("LB115 · interfaz",()=>{it("se abre tras LB114 y muestra fundamento y estados",()=>{for(const x of ['prior.status!=="HUMAN_VALIDATED"','Fundamento jurídico','x.status','Consultar BOE'])expect(LB115_PLANNED_MODIFICATION_SCRIPT).toContain(x);});it("persiste siete componentes y consentimiento humano",()=>{for(const p of ["modificationPercent","economic.modificationAmountExVatCents","execution.plannedModificationRegime","execution.plannedModificationJustification","execution.plannedModificationProcedure","execution.plannedModificationNoNewUnitPrices","execution.plannedModificationValueEstimatedTreatment"])expect(LB115_PLANNED_MODIFICATION_SCRIPT).toContain(`validate(id,"${p}"`);expect(LB115_PLANNED_MODIFICATION_SCRIPT).toContain("lb115-human-validation");});});
