import {describe,expect,it} from "vitest";import {evaluateTechnicalSpecifications} from "../src/application/intake/lb118/TechnicalSpecificationsEngine";
const req={id:"RT-01",category:"Funcional",requirement:"Capacidad mínima de 500 unidades por hora medida en condiciones nominales.",verificationMethod:"Ficha técnica y prueba funcional durante la recepción.",justification:"La carga de trabajo validada exige procesar 4.000 unidades en una jornada de ocho horas.",brandOrOriginReference:"",brandReferenceJustification:"",equivalentAcceptanceCriteria:""};
const base={contractType:"SUPPLY" as const,technicalPurpose:"Procesar y registrar las unidades recibidas con trazabilidad.",requirements:[req],usedByNaturalPersons:true,accessibilityRegime:"APPLIES" as const,accessibilityRequirements:"Interfaz compatible con teclado y lector de pantalla conforme a la norma declarada.",accessibilityNotApplicableJustification:"",environmentAffected:false,environmentalRequirements:"",specificVariant:"ORDINARY_GLOBAL_PRICE",specialRequirements:"Entrega, configuración y prueba funcional del equipo."};
describe("LB118 · prescripciones técnicas",()=>{it("produce requisitos medibles con verificación y overlay",()=>{const r=evaluateTechnicalSpecifications(base);expect(r.technicalRequirements).toHaveLength(1);expect(r.verificationMethods[0]).toContain("prueba funcional");expect(r.documentaryStatements.find(x=>x.element==="SPECIAL_OVERLAY")?.status).toBe("APPLIES");});it("rechaza requisitos vacíos, duplicados o vagos",()=>{expect(()=>evaluateTechnicalSpecifications({...base,requirements:[]})).toThrow(/prescripción/i);expect(()=>evaluateTechnicalSpecifications({...base,requirements:[req,req]})).toThrow(/duplicado/i);expect(()=>evaluateTechnicalSpecifications({...base,requirements:[{...req,requirement:"Producto de alta calidad"}]})).toThrow(/indeterminada/i);});it("exige justificación y equivalencia para una referencia de marca u origen",()=>{expect(()=>evaluateTechnicalSpecifications({...base,requirements:[{...req,brandOrOriginReference:"Marca X"}]})).toThrow(/justificación/i);const r=evaluateTechnicalSpecifications({...base,requirements:[{...req,brandOrOriginReference:"Marca X",brandReferenceJustification:"No es posible describir con precisión la compatibilidad del componente existente mediante especificaciones generales.",equivalentAcceptanceCriteria:"idéntica interfaz, dimensiones, protocolo y rendimiento acreditados"}]});expect(r.equivalenceRegime).toContain("equivalentes");});it("exige motivación suficiente si se excluye accesibilidad para usuarios humanos",()=>{expect(()=>evaluateTechnicalSpecifications({...base,accessibilityRegime:"NOT_APPLICABLE",accessibilityRequirements:"",accessibilityNotApplicableJustification:"No procede"})).toThrow(/motivación suficiente/i);});});
