name: Test write permission

# TEMPORARY WORKFLOW RETIRED.
# Kept as an inert historical marker until repository-history cleanup is performed.
# It intentionally performs no writes and is not triggered automatically.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This temporary write-permission test is retired."
