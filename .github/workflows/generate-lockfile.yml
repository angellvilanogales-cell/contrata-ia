name: Generate package-lock

# RETIRED.
# package-lock.json is committed and validated by the canonical CI workflow.
# This workflow is intentionally inert to prevent automatic repository writes.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic lockfile generation is retired; use npm ci in CI."
