import http,{type ServerResponse}from"node:http";import path from"node:path";
import { UniversalEvidenceWorkspace,type UniversalEvidenceRecord } from "../../application/intake/lb52/UniversalEvidenceWorkspace";
import { evaluateConcessionPhysicalBaseline } from "../../application/intake/lb98/ConcessionPhysicalBaseline";
import { createHttpPersistedConcessionTemplateAssetStoreFromEnv } from "../../application/intake/lb98/ConcessionPersistedTemplateAssetStore";
import { evaluateConcessionRiskOperational } from "../../application/intake/lb98/ConcessionRiskOperationalGate";
import { generateConcessionUserDocumentPackage } from "../../application/intake/lb98/ConcessionUserDocumentPackageGenerator";
import { evaluateConcessionUserJourney } from "../../application/intake/lb98/ConcessionUserJourneyCoordinator";
import { evaluateConcessionVerticalClosure } from "../../application/intake/lb98/ConcessionVerticalClosureGate";
import { evaluateConcessionViabilityContent } from "../../application/intake/lb98/ConcessionViabilityContentGate";
import { DurableUniversalEvidenceWorkspace } from "../../application/universal/DurableUniversalEvidenceWorkspace";import { createUniversalCaseMirrorFromEnv } from "../../application/universal/HttpUniversalCaseMirror";import { UniversalDurableCaseStore } from "../../application/universal/UniversalDurableCaseStore";import { createLB97RuntimeServer } from "../lb97/LB97RuntimeServer";import { SecurityPolicy } from "../lb7/SecurityPolicy";import { CONCESSION_USER_JOURNEY_UI } from "./ConcessionUserJourneyUi";
const DATA_ROOT=path.resolve(process.env.CONTRATA_IA_DATA_DIR??"var/contrata-ia");const EVIDENCE_ROOT=path.join(DATA_ROOT,"universal-evidence-v1");const security=new SecurityPolicy();const localEvidence=new UniversalEvidenceWorkspace(EVIDENCE_ROOT);const durableEvidence=new DurableUniversalEvidenceWorkspace(EVIDENCE_ROOT,localEvidence,new UniversalDurableCaseStore(1,createUniversalCaseMirrorFromEnv()??undefined));
function sendJson(r:ServerResponse,s:number,v:unknown){const b=Buffer.from(JSON.stringify(v));r.writeHead(s,{"content-type":"application/json; charset=utf-8","content-length":b.length,"cache-control":"no-store"});r.end(b);}function sendHtml(r:ServerResponse,h:string){const b=Buffer.from(h);r.writeHead(200,{"content-type":"text/html; charset=utf-8","content-length":b.length,"cache-control":"no-store"});r.end(b);}function sendBinary(r:ServerResponse,d:Uint8Array,n:string,m:string){const b=Buffer.from(d);r.writeHead(200,{"content-type":m,"content-length":b.length,"content-disposition":`attachment; filename="${n}"`,"cache-control":"no-store"});r.end(b);}function statusFor(e:Error){if(/autenticación|credencial|sesión segura/i.test(e.message))return 401;if(/permiso insuficiente/i.test(e.message))return 403;if(/no encontrad/i.test(e.message))return 404;return 400;}function v(r:UniversalEvidenceRecord,p:string){return r.fields[p]?.value;}
function legalGates(record:UniversalEvidenceRecord){const subtype=v(record,"concession.subtype")==="SERVICE_CONCESSION"||v(record,"concession.subtype")==="WORKS_CONCESSION"?v(record,"concession.subtype") as "SERVICE_CONCESSION"|"WORKS_CONCESSION":null;const conclusion=v(record,"concession.viabilityStudyConclusion");const stateAid=v(record,"concession.stateAidRelevant");const risk=evaluateConcessionRiskOperational({subtype,viabilityStudyApproved:v(record,"concession.viabilityStudyApproved")===true,viabilityStudyConcludesViable:conclusion==="VIABLE"?true:conclusion==="NOT_VIABLE"?false:null,demandRiskTransferred:v(record,"concession.demandRiskTransferred")===true,supplyRiskTransferred:v(record,"concession.supplyRiskTransferred")===true,marketExposureReal:v(record,"concession.marketExposureReal")===true,recoveryOfInvestmentGuaranteed:typeof v(record,"concession.investmentRecoveryGuaranteed")==="boolean"?v(record,"concession.investmentRecoveryGuaranteed") as boolean:null,recoveryOfCostsGuaranteed:typeof v(record,"concession.costRecoveryGuaranteed")==="boolean"?v(record,"concession.costRecoveryGuaranteed") as boolean:null,estimatedPotentialLossMoreThanNominal:v(record,"concession.potentialLossMoreThanNominal")===true,concessionRevenueModelDefined:Boolean(v(record,"concession.revenueModel")),netPresentValueAnalysisAvailable:Boolean(v(record,"concession.npvAnalysis")),stateAidRelevant:typeof stateAid==="boolean"?stateAid as boolean:null,stateAidCompatibilityAddressed:stateAid===false||Boolean(v(record,"concession.stateAidCompatibility"))});const viability=evaluateConcessionViabilityContent({subtype,concessionChoiceJustification:Boolean(v(record,"need")),demandForecast:Boolean(v(record,"concession.demandForecast")),investmentAndFinancingPlan:Boolean(v(record,"concession.investmentPlan")),operatingCostRevenueModel:Boolean(v(record,"concession.revenueModel")),netPresentValueAndDiscountRate:Boolean(v(record,"concession.npvAnalysis")),riskAllocationMatrix:Boolean(v(record,"concession.riskAllocation")),stateAidResolved:typeof stateAid==="boolean"&&(stateAid===false||Boolean(v(record,"concession.stateAidCompatibility"))});return{risk,viability};}

/** LB98 V1 cubre físicamente SERVICE_CONCESSION; WORKS_CONCESSION permanece separado hasta acreditar su overlay físico. */
export function createLB98RuntimeServer():http.Server{const base=createLB97RuntimeServer();return http.createServer(async(request,response)=>{security.applySecurityHeaders(response);try{const url=new URL(request.url??"/","http://localhost");const parts=url.pathname.split("/").filter(Boolean);if(request.method==="GET"&&(url.pathname==="/concession"||url.pathname==="/concession/")){sendHtml(response,CONCESSION_USER_JOURNEY_UI);return;}if(parts[0]==="api"&&parts[1]==="lb98"&&parts[2]==="cases"&&parts[3]){const caseId=decodeURIComponent(parts[3]);if(request.method==="GET"&&parts[4]==="journey"&&parts.length===5){const actor=security.authenticate(request);security.require(actor,"VIEWER");const restored=await durableEvidence.get(caseId);const store=createHttpPersistedConcessionTemplateAssetStoreFromEnv();const physical=store?await store.readiness():{ready:false,blockers:["Persistencia externa Concession no configurada."],assets:[]};const gates=legalGates(restored.record);const baseline=evaluateConcessionPhysicalBaseline();const closure=evaluateConcessionVerticalClosure({realCaseEvidenceReady:baseline.completeRealCaseLocated,risk:gates.risk,viabilityContent:gates.viability,pcapAvailable:physical.assets.some(a=>a.kind==="PCAP"&&a.available),memoryAvailable:physical.assets.some(a=>a.kind==="MEMORIA"&&a.available),pptAvailable:physical.assets.some(a=>a.kind==="PPT"&&a.available),viabilityTemplateAvailable:physical.assets.some(a=>a.kind==="VIABILITY"&&a.available),packageGeneratorReady:true,e2eReady:true});const journey=evaluateConcessionUserJourney(restored.record,closure.physicalPackageOperational);sendJson(response,200,{journey,concessionTemplates:physical,risk:gates.risk,viability:gates.viability,closure,realCaseEvidence:baseline.cases,sourceAuthority:"LCSP + Junta real concession cases Puerto Real/CFA",officialModelClaimed:false,humanValidationRequired:true});return;}if(request.method==="POST"&&parts[4]==="generate-package"&&parts.length===5){const actor=security.authenticate(request);security.require(actor,"OPERATOR");const restored=await durableEvidence.get(caseId);const store=createHttpPersistedConcessionTemplateAssetStoreFromEnv();if(!store){sendJson(response,503,{ready:false,blockers:["Persistencia externa Concession no configurada."]});return;}const physical=await store.readiness();const gates=legalGates(restored.record);const baseline=evaluateConcessionPhysicalBaseline();const closure=evaluateConcessionVerticalClosure({realCaseEvidenceReady:baseline.completeRealCaseLocated,risk:gates.risk,viabilityContent:gates.viability,pcapAvailable:physical.assets.some(a=>a.kind==="PCAP"&&a.available),memoryAvailable:physical.assets.some(a=>a.kind==="MEMORIA"&&a.available),pptAvailable:physical.assets.some(a=>a.kind==="PPT"&&a.available),viabilityTemplateAvailable:physical.assets.some(a=>a.kind==="VIABILITY"&&a.available),packageGeneratorReady:true,e2eReady:true});const journey=evaluateConcessionUserJourney(restored.record,closure.physicalPackageOperational);if(!closure.engineeringClosed||!journey.readyForDocuments){sendJson(response,409,{ready:false,blockers:[...new Set([...closure.blockers,...journey.blockers])],closure,journey});return;}const pkg=await generateConcessionUserDocumentPackage({record:restored.record,templateStore:store});if(!pkg.ready||!pkg.bytes||!pkg.fileName){sendJson(response,409,pkg);return;}sendBinary(response,pkg.bytes,pkg.fileName,pkg.mediaType);return;}}base.emit("request",request,response);}catch(error){const failure=error instanceof Error?error:new Error(String(error));if(!response.headersSent)sendJson(response,statusFor(failure),{error:failure.message});else response.end();}});}
