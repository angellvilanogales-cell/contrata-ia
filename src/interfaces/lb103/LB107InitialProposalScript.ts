export const LB107_INITIAL_PROPOSAL_SCRIPT = String.raw`(function(){
var ANSWERS_KEY="contrataIaAdaptiveAnswers",CASE_KEY="contrataIaAdaptiveCaseId";
function esc(v){return String(v==null?"":v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");}
function readAnswers(){try{return JSON.parse(sessionStorage.getItem(ANSWERS_KEY)||"{}");}catch(e){return {};}}
function saveAnswers(a,kind){sessionStorage.setItem(ANSWERS_KEY,JSON.stringify(a));localStorage.setItem(ANSWERS_KEY,JSON.stringify(a));document.dispatchEvent(new CustomEvent("contrata-ia:adaptive-saved",{detail:{kind:kind}}));}
function caseId(){return localStorage.getItem(CASE_KEY)||"";}
function legal(items,application){return '<details><summary>Fundamento jurídico y aplicación</summary>'+items.map(function(b){return '<div class="info"><strong>'+esc(b.norm)+' · artículo '+esc(b.article)+', apartado '+esc(b.paragraph)+'</strong><br>«'+esc(b.relevantOfficialExcerpt)+'»<br><span class="muted">Aplicación: '+esc(application)+'</span><br><a href="'+esc(b.officialUrl)+'" target="_blank" rel="noopener noreferrer">Consultar fuente oficial '+esc(b.sourceAuthority)+'</a></div>';}).join("")+'</details>';}
async function json(url,options){var r=await fetch(url,Object.assign({credentials:"same-origin",headers:{"content-type":"application/json"}},options||{}));var body=await r.json().catch(function(){return {};});if(!r.ok)throw new Error(body.error||"No se pudo completar la operación.");return body;}
async function validateEvidence(id,path,value){var root="/api/adaptive/cases/"+encodeURIComponent(id)+"/universal-evidence";await json(root,{method:"PUT",body:JSON.stringify({fieldPath:path,value:value,sourceId:"ui:lb107:initial-proposal",note:"Propuesta del aplicativo elegida o modificada expresamente por la persona responsable"})});await json(root+"/validate",{method:"POST",body:JSON.stringify({fieldPath:path})});}
function toggleLegacy(show){var old=document.getElementById("lb103GuidedPanel");if(old)old.style.display=show?"":"none";var work=document.getElementById("work");if(work)work.style.display="none";}
function parseLots(value){var seen={};return String(value||"").split(/\n+/).map(function(x){return x.trim();}).filter(Boolean).map(function(line,index){var parts=line.split("|");if(parts.length!==2||!parts[0].trim()||!parts[1].trim())throw new Error("Defina cada lote como: Lote 1 | descripción concreta de sus prestaciones.");var name=parts[0].trim(),key=name.toLowerCase();if(seen[key])throw new Error("Lote repetido: "+name+".");seen[key]=true;return{id:"LOT-"+(index+1),name:name,description:parts[1].trim()};});}
function candidateKey(lotId,code){return lotId+"::"+code;}
function cpvChoiceList(groups,state){var prior=Array.isArray(state.selectedCpvs)?state.selectedCpvs:[];return groups.map(function(group){return '<div class="info"><h4>'+esc(group.name)+'</h4><p class="muted">'+esc(group.description)+'</p>'+group.candidates.map(function(c,index){var key=candidateKey(group.id,c.code),wasSelected=prior.some(function(x){return x.lotId===group.id&&x.code===c.code;}),checked=(prior.length?wasSelected:index===0)?' checked':'',mainChecked=state.mainCpvKey===key||(prior.length===0&&group.id==="WHOLE"&&index===0);return '<div style="margin:10px 0"><label><input type="checkbox" class="lb107CpvChoice" data-lot="'+esc(group.id)+'" data-code="'+esc(c.code)+'" data-description="'+esc(c.officialDescription)+'" style="width:auto"'+checked+'> <strong>'+esc(c.code)+' · '+esc(c.officialDescription)+'</strong></label><label style="margin-left:28px"><input type="radio" name="lb107CpvMain" value="'+esc(key)+'" style="width:auto"'+(mainChecked?' checked':'')+'> Designar como CPV principal del expediente</label><div class="muted" style="margin-left:28px"><strong>Por qué puede encajar:</strong> '+esc(c.explanation)+'</div></div>';}).join("")+'</div>';}).join("");}
function currentDraft(){return{object:document.getElementById("lb107Object").value,scope:document.getElementById("lb107Scope").value,type:document.getElementById("lb107Type").value,need:document.getElementById("lb107Need").value,lotRaw:document.getElementById("lb107Lots").value,noLots:document.getElementById("lb107NoLots").value,lotDefinitions:document.getElementById("lb107LotDefinitions").value};}
function mount(){var main=document.querySelector("main"),work=document.getElementById("work");if(!main||!work||document.getElementById("lb107InitialBlock"))return;var panel=document.createElement("section");panel.id="lb107InitialBlock";main.insertBefore(panel,document.getElementById("lb106VirginPilot")||work);render(panel);}
function render(panel){var a=readAnswers(),s=a.__lb107||{},id=caseId();
if(!id){toggleLegacy(false);panel.innerHTML='<div class="card"><h2>Bloque inicial validado · análisis y propuesta</h2><p>Cree o abra un expediente. La única entrada inicial será la descripción de la necesidad; después el sistema propondrá y usted decidirá.</p></div>';return;}
if(s.status==="HUMAN_VALIDATED"){toggleLegacy(true);panel.innerHTML='<div class="card info"><h2>Bloque inicial validado</h2><p>La descripción fue analizada y la persona responsable validó objeto, tipo contractual, lotes, CPV por lote y necesidad.</p><button id="lb107ReviewLotsCpvs" type="button" class="secondary">Revisar lotes y CPV</button></div>';document.getElementById("lb107ReviewLotsCpvs").onclick=function(){s.status="REVIEW";a.__lb107=s;saveAnswers(a,"lb107-review-lots-cpv");render(panel);};return;}
toggleLegacy(false);
if(!s.proposal){panel.innerHTML='<div class="card"><h2>1. Describa qué se necesita</h2><p>Explique con lenguaje natural el resultado, bienes o trabajos que se quieren contratar. El aplicativo analizará la descripción y formulará propuestas; todavía no se registrará ninguna decisión.</p><textarea id="lb107Description" maxlength="6000">'+esc(s.description||"")+'</textarea><button id="lb107Analyze" type="button">Analizar y proponer</button><p id="lb107Status" class="muted"></p></div>';document.getElementById("lb107Analyze").onclick=async function(){var status=document.getElementById("lb107Status");try{var description=document.getElementById("lb107Description").value.trim();status.textContent="Analizando descripción...";s.description=description;s.proposal=await json("/api/lb107/initial-proposal",{method:"POST",body:JSON.stringify({description:description})});s.draft=null;s.cpvGroups=null;a.__lb107=s;saveAnswers(a,"lb107-proposal");render(panel);}catch(e){status.textContent=e.message;status.className="warning";}};return;}
var p=s.proposal,d=s.draft||{},lotValue=d.lotRaw==null?(p.lots.recommended?"true":"false"):d.lotRaw;
var proposedLotDefinitions=Array.isArray(p.lots.suggestedDefinitions)?p.lots.suggestedDefinitions.map(function(lot){return lot.name+" | "+lot.description;}).join("\n"):"";
var wholeGroup=[{id:"WHOLE",name:"Expediente sin división en lotes",description:p.sourceDescription,candidates:p.cpvCandidates}];
var groups=lotValue==="true"?(s.cpvGroups||[]):wholeGroup;
panel.innerHTML='<div class="card"><h2>2. Valore y elija las propuestas</h2><p class="warning"><strong>No son decisiones automáticas.</strong> La división en lotes se decide antes de seleccionar los CPV. Solo se registrará lo que confirme la persona responsable.</p></div>'+
'<div class="card"><h3>Objeto y alcance</h3><label><strong>Objeto propuesto</strong></label><textarea id="lb107Object">'+esc(d.object==null?p.objectDraft:d.object)+'</textarea><label><strong>Trabajos, entregas o alcance propuesto</strong></label><textarea id="lb107Scope">'+esc(d.scope==null?p.scopeDraft:d.scope)+'</textarea>'+legal(p.legalBasisByDecision.object,"El objeto debe describir de forma determinada la prestación completa declarada.")+'</div>'+
'<div class="card"><h3>Tipo de contrato</h3><p>'+esc(p.contractType.reasoning)+' · Confianza '+esc(p.contractType.confidence)+'</p><select id="lb107Type"><option value="SUPPLY"'+((d.type||p.contractType.recommended)==="SUPPLY"?' selected':'')+'>Suministro</option><option value="SERVICE"'+((d.type||p.contractType.recommended)==="SERVICE"?' selected':'')+'>Servicio</option></select>'+legal(p.legalBasisByDecision.contractType,"La elección se realiza por la prestación principal descrita.")+'</div>'+
'<div class="card"><h3>División en lotes</h3><p>'+esc(p.lots.reasoning)+' · Confianza '+esc(p.lots.confidence)+'</p><select id="lb107Lots"><option value="true"'+(lotValue==="true"?' selected':'')+'>Dividir en lotes diferenciados</option><option value="false"'+(lotValue==="false"?' selected':'')+'>Lote único</option><option value="pending"'+(lotValue==="pending"?' selected':'')+'>No dispongo aún de hechos suficientes</option></select><label><strong>Propuesta de lotes del sistema</strong> (puede aceptarla sin escribir o modificarla antes de validarla)</label><textarea id="lb107LotDefinitions" placeholder="El sistema propondrá aquí los lotes cuando identifique prestaciones diferenciadas">'+esc(d.lotDefinitions==null?proposedLotDefinitions:d.lotDefinitions)+'</textarea><button id="lb107SuggestLotCpvs" type="button" class="secondary">Aceptar lotes y sugerir CPV</button><label><strong>Motivación concreta si se elige lote único</strong></label><textarea id="lb107NoLots">'+esc(d.noLots==null?(p.lots.noDivisionJustificationDraft||""):d.noLots)+'</textarea>'+legal(p.legalBasisByDecision.lots,"Primero se determina la configuración en lotes. Después se clasifican las prestaciones de cada lote mediante CPV.")+'<p id="lb107LotStatus" class="muted"></p></div>'+
'<div class="card"><h3>CPV sugeridos para selección humana</h3>'+(lotValue==="true"&&!groups.length?'<p class="warning">Defina los lotes y pulse «Analizar lotes y sugerir CPV». Los códigos no se introducen manualmente.</p>':cpvChoiceList(groups,s))+legal(p.legalBasisByDecision.cpv,"Cada opción muestra código y denominación oficial completos. La persona puede marcar uno o varios CPV por lote y debe designar el CPV principal del expediente.")+'</div>'+
'<div class="card"><h3>Necesidad e idoneidad</h3><textarea id="lb107Need">'+esc(d.need==null?p.needDraft:d.need)+'</textarea>'+legal(p.legalBasisByDecision.need,"La unidad promotora debe corregir cualquier hecho o conclusión que no pueda acreditar.")+'<button id="lb107Validate" type="button">Confirmar decisiones iniciales</button><button id="lb107Reanalyze" type="button" class="secondary">Modificar descripción y volver a analizar</button><p id="lb107FinalStatus" class="muted"></p></div>';
document.getElementById("lb107Lots").onchange=function(){s.draft=currentDraft();s.cpvGroups=null;a.__lb107=s;saveAnswers(a,"lb107-lots-change");render(panel);};
document.getElementById("lb107SuggestLotCpvs").onclick=async function(){var st=document.getElementById("lb107LotStatus");try{s.draft=currentDraft();if(s.draft.lotRaw!=="true")throw new Error("Esta acción solo procede cuando se ha elegido dividir en lotes.");var lots=parseLots(s.draft.lotDefinitions);if(lots.length<2)throw new Error("La división requiere al menos dos lotes descritos.");st.textContent="Analizando separadamente las prestaciones de cada lote...";var results=await Promise.all(lots.map(function(lot){return json("/api/lb107/initial-proposal",{method:"POST",body:JSON.stringify({description:lot.description})});}));s.cpvGroups=lots.map(function(lot,index){return{id:lot.id,name:lot.name,description:lot.description,candidates:results[index].cpvCandidates};});a.__lb107=s;saveAnswers(a,"lb107-lot-cpv-proposals");render(panel);}catch(e){st.textContent=e.message;st.className="warning";}};
document.getElementById("lb107Reanalyze").onclick=function(){s.proposal=null;s.draft=null;s.cpvGroups=null;a.__lb107=s;saveAnswers(a,"lb107-reanalyze");render(panel);};
document.getElementById("lb107Validate").onclick=async function(){var status=document.getElementById("lb107FinalStatus"),button=this;try{var draft=currentDraft(),object=draft.object.trim(),scope=draft.scope.trim(),type=draft.type,need=draft.need.trim(),lotRaw=draft.lotRaw,noLots=draft.noLots.trim();if(!object||!scope||!need)throw new Error("Revise objeto, alcance y necesidad: no pueden quedar vacíos.");if(lotRaw==="pending"){s.pendingClarifications=["lots.divisionIntoLots"];a.__lb107=s;saveAnswers(a,"lb107-pending-clarification");throw new Error("La respuesta pendiente se ha guardado, pero no se validará ni permitirá generar.");}var lots=lotRaw==="true",definitions=lots?parseLots(draft.lotDefinitions):[];if(lots&&definitions.length<2)throw new Error("Defina al menos dos lotes antes de elegir los CPV.");if(lots&&(!s.cpvGroups||s.cpvGroups.length!==definitions.length))throw new Error("Analice los lotes para obtener sus CPV sugeridos antes de confirmar.");if(!lots&&!noLots)throw new Error("El lote único exige una motivación concreta.");var checked=[].slice.call(document.querySelectorAll(".lb107CpvChoice:checked")),main=document.querySelector('input[name="lb107CpvMain"]:checked');if(!checked.length)throw new Error("Seleccione al menos un CPV.");if(!main)throw new Error("Designe cuál será el CPV principal del expediente.");var selected=checked.map(function(el){return{lotId:el.getAttribute("data-lot"),code:el.getAttribute("data-code"),officialDescription:el.getAttribute("data-description")};});var mainKey=main.value,mainSelected=selected.find(function(x){return candidateKey(x.lotId,x.code)===mainKey;});if(!mainSelected)throw new Error("El CPV principal también debe estar marcado para su inclusión.");var assignments=lots?definitions.map(function(lot){var cpvs=selected.filter(function(x){return x.lotId===lot.id;}).map(function(x){return{code:x.code,officialDescription:x.officialDescription};});if(!cpvs.length)throw new Error("Seleccione al menos un CPV para "+lot.name+".");return{lotId:lot.id,lot:lot.name,description:lot.description,cpvs:cpvs,cpvCodes:cpvs.map(function(x){return x.code;})};}):[];var additional=selected.filter(function(x){return x.code!==mainSelected.code;}).filter(function(x,index,array){return array.findIndex(function(y){return y.code===x.code;})===index;}).map(function(x){return{code:x.code,officialDescription:x.officialDescription};});s.draft=draft;s.selectedCpvs=selected;s.mainCpvKey=mainKey;button.disabled=true;status.textContent="Registrando cada elección y su validación humana...";await validateEvidence(id,"object",object);await validateEvidence(id,"contractType",type);await validateEvidence(id,"cpvMain",mainSelected.code);await validateEvidence(id,"cpvMainDescription",mainSelected.officialDescription);await validateEvidence(id,"cpvAdditional",additional);await validateEvidence(id,"lots.cpvAssignments",assignments);await validateEvidence(id,"need",need);await validateEvidence(id,"lots.divisionIntoLots",lots);if(lots)await validateEvidence(id,"lots.lots",definitions);else await validateEvidence(id,"lots.noDivisionJustification",noLots);a=readAnswers();s=a.__lb107||s;s.status="HUMAN_VALIDATED";s.draft=draft;s.selectedCpvs=selected;s.mainCpvKey=mainKey;s.validated={description:p.sourceDescription,object:object,scope:scope,contractType:type,cpvMain:mainSelected,cpvAdditional:additional,cpvByLot:assignments,need:need,divisionIntoLots:lots,lots:definitions,noDivisionJustification:lots?undefined:noLots};s.pendingClarifications=[];a.needAndPurpose=need;a.scopeDetail=scope;a.technicalContinuity=lots?"SEPARABLE":"SAME_CONTRACTOR_PREFERRED";a.object=object;a.cpv=mainSelected.code;a.cpvDescription=mainSelected.officialDescription;a.cpvAdditional=additional;a.cpvByLot=assignments;a.dividedIntoLots=lots;a.contractType=type;a.need=need;var guided=a.__lb103&&typeof a.__lb103==="object"?a.__lb103:{decisions:{},phase:"GUIDED_DECISIONS"};guided.contractType=type;guided.decisions=guided.decisions||{};guided.decisions["common:object"]={status:"HUMAN_VALIDATED",value:object,evidenceFieldPath:"object",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:cpv"]={status:"HUMAN_VALIDATED",value:mainSelected.code,evidenceFieldPath:"cpvMain",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:cpv-additional"]={status:"HUMAN_VALIDATED",value:additional,evidenceFieldPath:"cpvAdditional",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:cpv-by-lot"]={status:"HUMAN_VALIDATED",value:assignments,evidenceFieldPath:"lots.cpvAssignments",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:lots"]={status:"HUMAN_VALIDATED",value:lots,evidenceFieldPath:"lots.divisionIntoLots",validatedThrough:"UNIVERSAL_EVIDENCE_API"};a.__lb103=guided;a.__lb107=s;saveAnswers(a,"lb107-human-validation");render(panel);}catch(e){button.disabled=false;status.textContent=e.message;status.className="warning";}};
}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",mount);else mount();
document.addEventListener("contrata-ia:adaptive-saved",function(e){if(e.detail&&String(e.detail.kind||"").indexOf("lb107")===0)return;setTimeout(function(){var p=document.getElementById("lb107InitialBlock");if(p)render(p);},0);});
})();`;
