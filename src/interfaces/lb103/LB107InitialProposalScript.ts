export const LB107_INITIAL_PROPOSAL_SCRIPT = String.raw`(function(){
var ANSWERS_KEY="contrataIaAdaptiveAnswers",CASE_KEY="contrataIaAdaptiveCaseId";
function esc(v){return String(v==null?"":v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");}
function readAnswers(){try{return JSON.parse(sessionStorage.getItem(ANSWERS_KEY)||"{}");}catch(e){return {};}}
function saveAnswers(a,kind){sessionStorage.setItem(ANSWERS_KEY,JSON.stringify(a));localStorage.setItem(ANSWERS_KEY,JSON.stringify(a));document.dispatchEvent(new CustomEvent("contrata-ia:adaptive-saved",{detail:{kind:kind}}));}
function caseId(){return localStorage.getItem(CASE_KEY)||"";}
function legal(items,application){return '<details><summary>Fundamento jurídico y aplicación</summary>'+items.map(function(b){return '<div class="info"><strong>'+esc(b.norm)+' · artículo '+esc(b.article)+', apartado '+esc(b.paragraph)+'</strong><br>«'+esc(b.relevantOfficialExcerpt)+'»<br><span class="muted">Aplicación: '+esc(application)+'</span><br><a href="'+esc(b.officialUrl)+'" target="_blank" rel="noopener noreferrer">Consultar fuente oficial '+esc(b.sourceAuthority)+'</a></div>';}).join("")+'</details>';}
function cpvCodes(value){var seen={};return String(value||"").split(/[\s,;]+/).map(function(x){return x.trim();}).filter(Boolean).map(function(code){if(!/^\d{8}-\d$/.test(code))throw new Error("Código CPV no válido: "+code+". Use el formato 00000000-0.");if(seen[code])throw new Error("Código CPV duplicado: "+code+".");seen[code]=true;return code;});}
function lotAssignments(value,allowed){var seenLots={};return String(value||"").split(/\n+/).map(function(x){return x.trim();}).filter(Boolean).map(function(line){var parts=line.split("|");if(parts.length!==2||!parts[0].trim())throw new Error("Cada lote debe escribirse como: Lote 1 | 00000000-0, 11111111-1.");var lot=parts[0].trim(),key=lot.toLowerCase(),codes=cpvCodes(parts[1]);if(seenLots[key])throw new Error("Lote repetido: "+lot+".");if(!codes.length)throw new Error("Asigne al menos un CPV a "+lot+".");codes.forEach(function(code){if(allowed.indexOf(code)<0)throw new Error(code+" debe seleccionarse antes como CPV principal o complementario.");});seenLots[key]=true;return{lot:lot,cpvCodes:codes};});}
async function json(url,options){var r=await fetch(url,Object.assign({credentials:"same-origin",headers:{"content-type":"application/json"}},options||{}));var body=await r.json().catch(function(){return {};});if(!r.ok)throw new Error(body.error||"No se pudo completar la operación.");return body;}
async function validateEvidence(id,path,value){var root="/api/adaptive/cases/"+encodeURIComponent(id)+"/universal-evidence";await json(root,{method:"PUT",body:JSON.stringify({fieldPath:path,value:value,sourceId:"ui:lb107:initial-proposal",note:"Propuesta del aplicativo elegida o modificada expresamente por la persona responsable"})});await json(root+"/validate",{method:"POST",body:JSON.stringify({fieldPath:path})});}
function toggleLegacy(show){var old=document.getElementById("lb103GuidedPanel");if(old)old.style.display=show?"":"none";var work=document.getElementById("work");if(work)work.style.display="none";}
function mount(){var main=document.querySelector("main"),work=document.getElementById("work");if(!main||!work||document.getElementById("lb107InitialBlock"))return;var panel=document.createElement("section");panel.id="lb107InitialBlock";main.insertBefore(panel,document.getElementById("lb106VirginPilot")||work);render(panel);}
function render(panel){var a=readAnswers(),s=a.__lb107||{},id=caseId();if(!id){toggleLegacy(false);panel.innerHTML='<div class="card"><h2>Bloque inicial validado · análisis y propuesta</h2><p>Cree o abra un expediente. La única entrada inicial será la descripción de la necesidad; después el sistema propondrá y usted decidirá.</p></div>';return;}if(s.status==="HUMAN_VALIDATED"){toggleLegacy(true);panel.innerHTML='<div class="card info"><h2>Bloque inicial validado</h2><p>La descripción fue analizada y la persona responsable validó o modificó objeto, tipo contractual, CPV, necesidad y lotes. Las decisiones siguientes se mostrarán sin repetir estas preguntas.</p><button id="lb107Review" type="button" class="secondary">Revisar bloque inicial</button></div>';document.getElementById("lb107Review").onclick=function(){s.status="REVIEW";a.__lb107=s;saveAnswers(a,"lb107-review");render(panel);};return;}toggleLegacy(false);if(!s.proposal){panel.innerHTML='<div class="card"><h2>1. Describa qué se necesita</h2><p>Explique con lenguaje natural el resultado, bienes o trabajos que se quieren contratar. El aplicativo analizará la descripción y formulará propuestas; todavía no se registrará ninguna decisión.</p><textarea id="lb107Description" maxlength="6000" placeholder="Ej.: Necesitamos adquirir...">'+esc(s.description||"")+'</textarea><button id="lb107Analyze" type="button">Analizar y proponer</button><p id="lb107Status" class="muted"></p></div>';document.getElementById("lb107Analyze").onclick=async function(){var status=document.getElementById("lb107Status");try{var description=document.getElementById("lb107Description").value.trim();status.textContent="Analizando descripción y catálogo CPV...";s.description=description;s.proposal=await json("/api/lb107/initial-proposal",{method:"POST",body:JSON.stringify({description:description})});a.__lb107=s;saveAnswers(a,"lb107-proposal");render(panel);}catch(e){status.textContent=e.message;status.className="warning";}};return;}
var p=s.proposal,cpvOptions=p.cpvCandidates.map(function(c){return '<option value="'+esc(c.code)+'">'+esc(c.code)+' · '+esc(c.officialDescription)+' · confianza '+esc(c.confidence)+'</option>';}).join("");
panel.innerHTML='<div class="card"><h2>2. Valore y elija las propuestas</h2><p class="warning"><strong>No son decisiones automáticas.</strong> Revise cada texto y modifíquelo cuando no refleje la realidad. Solo se registrarán al pulsar el botón final.</p></div>'+
'<div class="card"><h3>Objeto y alcance</h3><label><strong>Objeto propuesto</strong></label><textarea id="lb107Object">'+esc(p.objectDraft)+'</textarea><label><strong>Trabajos, entregas o alcance propuesto</strong></label><textarea id="lb107Scope">'+esc(p.scopeDraft)+'</textarea>'+legal(p.legalBasisByDecision.object,"El objeto debe describir de forma determinada la prestación completa que se acaba de declarar.")+'</div>'+
'<div class="card"><h3>Tipo de contrato</h3><p>'+esc(p.contractType.reasoning)+' · Confianza '+esc(p.contractType.confidence)+'</p><select id="lb107Type"><option value="SUPPLY"'+(p.contractType.recommended==="SUPPLY"?' selected':'')+'>Suministro</option><option value="SERVICE"'+(p.contractType.recommended==="SERVICE"?' selected':'')+'>Servicio</option></select>'+legal(p.legalBasisByDecision.contractType,"La elección se realiza por la prestación principal descrita, no por el nombre que se dé al expediente.")+'</div>'+
'<div class="card"><h3>CPV principal, complementarios y por lote</h3><p>Elija el código principal. Puede añadir varios códigos complementarios cuando describan prestaciones diferenciadas. Si habrá lotes, asigne uno o varios CPV a cada lote.</p><label><strong>CPV principal</strong></label><select id="lb107Cpv"><option value="">Seleccione un código</option>'+cpvOptions+'</select><label><strong>CPV complementarios</strong> (separados por comas o saltos de línea)</label><textarea id="lb107CpvAdditional" placeholder="Ej.: 80580000-3, 79634000-7"></textarea><label><strong>Asignación de CPV por lote</strong> (solo si se divide en lotes; una línea por lote)</label><textarea id="lb107LotCpvs" placeholder="Lote 1 | 80580000-3\nLote 2 | 79634000-7, 80580000-3"></textarea>'+p.cpvCandidates.map(function(c){return '<p><strong>'+esc(c.code)+' · '+esc(c.officialDescription)+'</strong><br><span class="muted">'+esc(c.explanation)+'</span></p>';}).join("")+legal(p.legalBasisByDecision.cpv,"Puede existir un CPV principal y códigos complementarios. Cuando se configuren lotes, la selección debe reflejar de forma coherente las prestaciones de cada lote y queda sometida a validación humana.")+'</div>'+
'<div class="card"><h3>Necesidad e idoneidad</h3><textarea id="lb107Need">'+esc(p.needDraft)+'</textarea>'+legal(p.legalBasisByDecision.need,"El aplicativo estructura una motivación desde los hechos descritos; la unidad promotora debe corregir cualquier hecho o conclusión que no pueda acreditar.")+'</div>'+
'<div class="card"><h3>División en lotes</h3><p>'+esc(p.lots.reasoning)+' · Confianza '+esc(p.lots.confidence)+'</p><select id="lb107Lots"><option value="true"'+(p.lots.recommended?' selected':'')+'>Dividir o estudiar lotes diferenciados</option><option value="false"'+(!p.lots.recommended?' selected':'')+'>Lote único</option><option value="pending">No dispongo aún de hechos suficientes</option></select><label><strong>Motivación concreta si se elige lote único</strong></label><textarea id="lb107NoLots">'+esc(p.lots.noDivisionJustificationDraft||"")+'</textarea>'+legal(p.legalBasisByDecision.lots,"Debe contrastarse si las partes pueden ejecutarse independientemente; la falta de datos queda pendiente y nunca se valida como respuesta final.")+'<button id="lb107Validate" type="button">Confirmar decisiones iniciales</button><button id="lb107Reanalyze" type="button" class="secondary">Modificar descripción y volver a analizar</button><p id="lb107FinalStatus" class="muted"></p></div>';
document.getElementById("lb107Reanalyze").onclick=function(){s.proposal=null;a.__lb107=s;saveAnswers(a,"lb107-reanalyze");render(panel);};
document.getElementById("lb107Validate").onclick=async function(){var status=document.getElementById("lb107FinalStatus"),button=this;try{var object=document.getElementById("lb107Object").value.trim(),scope=document.getElementById("lb107Scope").value.trim(),type=document.getElementById("lb107Type").value,cpv=document.getElementById("lb107Cpv").value,need=document.getElementById("lb107Need").value.trim(),lotRaw=document.getElementById("lb107Lots").value,noLots=document.getElementById("lb107NoLots").value.trim();if(!object||!scope||!need||!cpv)throw new Error("Revise objeto, alcance, CPV principal y necesidad: no pueden quedar vacíos.");if(lotRaw==="pending"){s.pendingClarifications=["lots.divisionIntoLots"];a.__lb107=s;saveAnswers(a,"lb107-pending-clarification");throw new Error("La respuesta pendiente se ha guardado. Puede continuar aclarando la separabilidad, pero no se validará ni permitirá generar.");}var lots=lotRaw==="true",additional=cpvCodes(document.getElementById("lb107CpvAdditional").value),allCpvs=[cpv].concat(additional);if(additional.indexOf(cpv)>=0)throw new Error("El CPV principal no debe repetirse como complementario.");var assignments=lotAssignments(document.getElementById("lb107LotCpvs").value,allCpvs);if(lots&&!assignments.length)throw new Error("Al dividir en lotes debe asignar uno o varios CPV a cada lote.");if(!lots&&assignments.length)throw new Error("La asignación de CPV por lote solo procede cuando existe división en lotes.");if(!lots&&!noLots)throw new Error("El lote único exige una motivación concreta basada en las características del objeto.");button.disabled=true;status.textContent="Registrando cada elección y su validación humana...";await validateEvidence(id,"object",object);await validateEvidence(id,"contractType",type);await validateEvidence(id,"cpvMain",cpv);await validateEvidence(id,"cpvAdditional",additional);await validateEvidence(id,"lots.cpvAssignments",assignments);await validateEvidence(id,"need",need);await validateEvidence(id,"lots.divisionIntoLots",lots);if(!lots)await validateEvidence(id,"lots.noDivisionJustification",noLots);a=readAnswers();s=a.__lb107||s;s.status="HUMAN_VALIDATED";s.validated={description:p.sourceDescription,object:object,scope:scope,contractType:type,cpvMain:cpv,cpvAdditional:additional,cpvByLot:assignments,need:need,divisionIntoLots:lots,noDivisionJustification:lots?undefined:noLots};s.pendingClarifications=[];a.needAndPurpose=need;a.scopeDetail=scope;a.technicalContinuity=lots?"SEPARABLE":"SAME_CONTRACTOR_PREFERRED";a.object=object;a.cpv=cpv;a.cpvAdditional=additional;a.cpvByLot=assignments;a.dividedIntoLots=lots;a.contractType=type;a.need=need;var guided=a.__lb103&&typeof a.__lb103==="object"?a.__lb103:{decisions:{},phase:"GUIDED_DECISIONS"};guided.contractType=type;guided.decisions=guided.decisions||{};guided.decisions["common:object"]={status:"HUMAN_VALIDATED",value:object,evidenceFieldPath:"object",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:cpv"]={status:"HUMAN_VALIDATED",value:cpv,evidenceFieldPath:"cpvMain",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:cpv-additional"]={status:"HUMAN_VALIDATED",value:additional,evidenceFieldPath:"cpvAdditional",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:cpv-by-lot"]={status:"HUMAN_VALIDATED",value:assignments,evidenceFieldPath:"lots.cpvAssignments",validatedThrough:"UNIVERSAL_EVIDENCE_API"};guided.decisions["common:lots"]={status:"HUMAN_VALIDATED",value:lots,evidenceFieldPath:"lots.divisionIntoLots",validatedThrough:"UNIVERSAL_EVIDENCE_API"};if(!lots)guided.decisions["common:no-lots-justification"]={status:"HUMAN_VALIDATED",value:noLots,evidenceFieldPath:"lots.noDivisionJustification",validatedThrough:"UNIVERSAL_EVIDENCE_API"};a.__lb103=guided;a.__lb107=s;saveAnswers(a,"lb107-human-validation");render(panel);}catch(e){button.disabled=false;status.textContent=e.message;status.className="warning";}};
}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",mount);else mount();
document.addEventListener("contrata-ia:adaptive-saved",function(e){if(e.detail&&String(e.detail.kind||"").indexOf("lb107")===0)return;setTimeout(function(){var p=document.getElementById("lb107InitialBlock");if(p)render(p);},0);});
})();`;
