# PENDIENTE DE VALIDACIÓN / FUENTE
#
# Este fichero conserva la ruta histórica de una fuente que se presentaba
# como RULE.SOLVENCIA pero que no constituye YAML válido para el runtime:
# contiene claves raíz repetidas y valores que no cumplen la estructura YAML
# declarativa utilizada por el conocimiento activo.
#
# NO se considera regla jurídica de producción.
# El contenido original permanece en el historial Git de este archivo.
# Debe recuperarse y normalizarse únicamente tras validar su correspondencia
# con la documentación maestra y la normativa aplicable.

status: pending-validation
canonical: false
code: RULE.SOLVENCIA
source_type: legacy-knowledge-source
reason: invalid-yaml-and-unvalidated-production-claim
human_validation_required: true
